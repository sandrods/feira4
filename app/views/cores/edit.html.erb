<%= page_header "Editar Cor" do %>

	<%= link_to icon(:chevron_left, "Voltar"), cores_path, class: 'btn' %>

	<%= link_to icon(:trash, 'Apagar esta Cor'), 
				@cor, 
				method: :delete, 
				data: { confirm: 'Tem certeza?' }, 
				class: 'btn btn-sm btn-danger' %>

<% end %>

<%= render 'form' %>
