<%= page_header "Tamanhos" %>

<div class="row">

  <div class="col-md-8">
    <table class="table table-hover table-striped">
      <thead>
        <tr>
          <th>Nome</th>
        </tr>
      </thead>
      <tbody>
        <% @tamanhos.each do |tamanho| %>
          <tr>
            <td><%= link_to tamanho.nome, edit_tamanho_path(tamanho) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
    
    <p class="index_count"><span><%= @tamanhos.count %></span> tamanhos encontradas.</p>

    <%= link_to(icon(:plus, 'Novo Tamanho'), new_tamanho_path, class: 'btn btn-primary') %>
  
  </div>

  <div class="col-md-4">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">Filtros</h3>
      </div>
      <div id="filtro_pedidos" class="panel-body">
        <%= simple_form_for @search, url: tamanhos_path, method: 'GET' do |f| %>
          <%= f.input :nome_cont, label: "Nome" %>

          <%= f.submit 'Filtrar', class: 'btn btn-info' %>
          <%= button_tag 'Limpar', class: 'btn btn-default clear' %>
        <% end %>
      </div>
    </div>
  </div>
</div>

