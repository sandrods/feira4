<tr>
  <td class="date"><%= link_to(registro.data.try(:strftime, '%d/%m/%Y'), edit_registro_path(registro), :class => "edit_reg" )%></td>
  <td><%= registro.descricao %></td>
  <td class='text-right <%= registro.cd %>' ><%= number_to_currency(registro.valor_cd) %></td>
</tr>